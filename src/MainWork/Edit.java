package MainWork;

import org.w3c.dom.Document;

/**
 * Função que tem como objetivo editar o ficheiro XML
 * 
 * @author Brogueira
 * 
 */ 

public class Edit {

	public Edit(Document doc, String mail, SignIN client) throws Exception {
	//	Remove r = new Remove (doc, mail,frame);
	//	AddToFile add = new AddToFile(doc, client);
	}

	
}
